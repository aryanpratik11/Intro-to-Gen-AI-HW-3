This repository implements two core NLP tasks: (1) Transformer components from scratch (scaled dot-product attention in NumPy, PyTorch encoder, adaptive attention, and time-series forecasting comparison with RNNs) and (2) Neural Machine Translation (RNN encoder-decoder with attention for English-French translation, including tokenization, masking, teacher forcing, and BLEU evaluation, with an optional Transformer extension).
